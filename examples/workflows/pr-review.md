# Local PR Review

```bash
pip install chronicle-sdk
chronicle pr-review --base main
```

Chronicle writes a local markdown review with changed files, impacted symbols, risk, warnings, and suggested tests.
