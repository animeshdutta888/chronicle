# Cursor Workflow

```bash
pip install chronicle-sdk
chronicle setup cursor
chronicle run "Fix auth token refresh bug"
chronicle finish
```

`chronicle setup cursor` updates `.cursor/rules/chronicle.mdc` and writes project MCP config to `.cursor/mcp.json`.
